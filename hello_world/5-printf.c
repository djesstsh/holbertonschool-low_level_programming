#include <stdio.h>

/**
 * main - Entry point
 *
 * Description:Calls printf and receives value returned
 *
 * Return:Always return 0 to indicate success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
