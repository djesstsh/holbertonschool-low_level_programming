#include <stdio.h>

/*
 * main - Entry point
 * Description : Calls puts and receives value returned
 * Return : Always return 0 to indicate success
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
